# Streamlit App

This project includes a simple Streamlit application for interactive dashboards and UI components.

## 📦 Installation

Make sure you have Python 3.8+ and pip installed. Then install Streamlit inside your virtual environment:

```bash
pip install streamlit

import streamlit as st
st.write("""
# My first app
Hello *world!*
""")

streamlit run quickst.py
